services:
  app:
    build:
      context: .
    container_name: vitest-component-test-tutorial
    volumes:
      - type: bind
        source: .
        target: /app
      - type: volume
        target: /app/node_modules
    stdin_open: true
    tty: true
